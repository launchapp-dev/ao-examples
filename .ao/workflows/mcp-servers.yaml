mcp_servers:
  filesystem:
    command: npx
    args: ["-y", "@modelcontextprotocol/server-filesystem", "/Users/samishukri/ao-examples", "/Users/samishukri/brain"]

  github:
    command: npx
    args: ["-y", "gh-cli-mcp"]

  sequential-thinking:
    command: npx
    args: ["-y", "@modelcontextprotocol/server-sequential-thinking"]
